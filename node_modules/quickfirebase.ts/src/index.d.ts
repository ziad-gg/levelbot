declare const firebase: any;
declare function connect(DataBaseInfo: {}): Promise<void>;
declare class database {
    private DataBaseInfo;
    private options;
    private database;
    private rootRef;
    private default;
    constructor(Options: {
        name: string;
        default: {};
    });
    set(key: string): Promise<void>;
    findOne(key: string | string[]): Promise<{
        data: any;
        save: () => Promise<void>;
    }>;
    delete(key: string | string[]): Promise<void>;
    deleteAll(): Promise<void>;
}
//# sourceMappingURL=index.d.ts.map